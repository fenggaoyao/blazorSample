@inject IModel Model

<div title="Target Heart Rate" class="tile">
    <h1>THR指数： </h1>
    <h2>@Model.Thr.Low  &mdash; @Model.Thr.High BPM</h2>
</div>

@functions {
    protected override void OnAfterRender()
    {
        Model.Register(() => base.StateHasChanged());
        base.OnAfterRender();
    }
}